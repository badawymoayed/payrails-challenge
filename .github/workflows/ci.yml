name: API Test Automation CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run_symbol_tests:
    runs-on: ubuntu-latest
    env:
      ALPHAVANTAGE_API_KEY: ${{ secrets.ALPHAVANTAGE_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
      - name: Run Symbol Search Tests
        run: mvn clean test -Dtest=SymbolSearchTests

      - name: Upload Symbol ExtentReports Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ExtentReports-Report-Symbol
          path: target/surefire-reports/ExtentReport.html

      - name: Upload Symbol Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Allure-Results-Symbol
          path: target/allure-results

  run_time_series_tests:
    runs-on: ubuntu-latest
    needs: run_symbol_tests
    env:
      ALPHAVANTAGE_API_KEY: ${{ secrets.ALPHAVANTAGE_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
      - name: Wait for API Rate Limit Reset
        run: sleep 60 # Wait for 60 seconds to ensure the rate limit has reset
      - name: Run Time Series Tests
        run: mvn test -Dtest=TimeSeriesTests

      - name: Upload Time Series ExtentReports Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ExtentReports-Report-TimeSeries
          path: target/surefire-reports/ExtentReport.html

      - name: Upload Time Series Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Allure-Results-TimeSeries
          path: target/allure-results

  allure_report:
    runs-on: ubuntu-latest
    needs: [run_symbol_tests, run_time_series_tests]
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Download Allure Results
        uses: actions/download-artifact@v4
        with:
          name: Allure-Results-Symbol
          path: allure-results/symbol

      - name: Download Time Series Allure Results
        uses: actions/download-artifact@v4
        with:
          name: Allure-Results-TimeSeries
          path: allure-results/time_series

      - name: Generate Allure Report
        run: mvn allure:report

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
        with:
          name: Allure-Report
          path: target/allure-report